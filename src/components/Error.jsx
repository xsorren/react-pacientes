const Error = ({children}) => {
   
    return (
        <div className="bg-red-800 text-white uppercase font-bold mb-3 rounded-md text-center p-3">
            {children}
        </div>
    )
}

export default Error
